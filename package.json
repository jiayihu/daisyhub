{
  "name": "turnip-market",
  "version": "1.0.0",
  "description": "",
  "main": "dist/index.js",
  "scripts": {
    "clean": "shx rm -rf dist",
    "build": "npm run clean && tsc",
    "deploy": "npm run build && gcloud functions deploy islands --runtime nodejs10 --trigger-http",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "Jiayi Hu <jiayi.ghu@gmail.com>",
  "license": "MIT",
  "dependencies": {
    "express": "^4.17.1"
  },
  "devDependencies": {
    "@types/express": "^4.17.6",
    "@types/node": "^13.13.0",
    "shx": "^0.3.2",
    "typescript": "^3.8.3"
  }
}
